[package]
name = "sexta-feira-perception"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.40", features = ["sync", "rt", "rt-multi-thread", "time", "macros"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[profile.dev]
opt-level = 0
