# =========================================================
# Sexta-Feira OS — local kernel dependencies (lean & private)
# No cloud SDKs. No PyTorch. Inference + embeddings run on Ollama (local).
# =========================================================

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database ---
sqlalchemy==2.0.35
alembic==1.13.3

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0

# --- Config / env ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Local brain transport (Ollama over HTTP) ---
httpx==0.27.2

# --- Vector math for memory recall (lightweight, no torch) ---
numpy==2.1.1

# --- Dev / tests ---
pytest==8.3.3
pytest-asyncio==0.24.0
